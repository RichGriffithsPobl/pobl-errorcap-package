describe("ErrorCap class", () => {
  it("should be a singleton", () => {
    // Test the singleton pattern
  });

  it("should initialise with a connection string", () => {
    // Test the init method
  });

  it("should send an error", () => {
    // Test the sendError method
  });
});
